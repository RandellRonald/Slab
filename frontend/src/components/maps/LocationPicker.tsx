import "maplibre-gl/dist/maplibre-gl.css";
import * as maplibregl from "maplibre-gl";
import { MapPin, Navigation, Search } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { mapService } from "../../services/customerService";
import { Button } from "../ui/Button";

export interface PickedLocation { latitude: number; longitude: number; address: string; area?: string; city?: string; district?: string; state?: string; postal_code?: string; }
const INDIA_CENTER = { latitude: 10.5276, longitude: 76.2144 };
const OSM_STYLE = { version: 8 as const, sources: { osm: { type: "raster" as const, tiles: ["https://tile.openstreetmap.org/{z}/{x}/{y}.png"], tileSize: 256, attribution: "© OpenStreetMap contributors" } }, layers: [{ id: "osm", type: "raster" as const, source: "osm", minzoom: 0, maxzoom: 19 }] };

export function LocationPicker({ onConfirm }: { onConfirm: (location: PickedLocation) => void }) {
  const mapNode = useRef<HTMLDivElement>(null); const mapRef = useRef<maplibregl.Map | null>(null);
  const [center, setCenter] = useState(INDIA_CENTER); const [query, setQuery] = useState(""); const [address, setAddress] = useState("Move the map or search for an Indian site address."); const [fields, setFields] = useState({ area: "", city: "", district: "", state: "", postal_code: "" }); const [results, setResults] = useState<Array<{ display_name: string; lat: string; lon: string; address?: Record<string, string> }>>([]); const [error, setError] = useState<string | null>(null); const [searching, setSearching] = useState(false); const [reverseLoading, setReverseLoading] = useState(false);
  useEffect(() => { if (!mapNode.current || mapRef.current) return; const map = new maplibregl.Map({ container: mapNode.current, style: OSM_STYLE, center: [INDIA_CENTER.longitude, INDIA_CENTER.latitude], zoom: 6 }); mapRef.current = map; map.addControl(new maplibregl.NavigationControl(), "bottom-right"); map.on("moveend", () => { const c = map.getCenter(); const next = { latitude: Number(c.lat.toFixed(6)), longitude: Number(c.lng.toFixed(6)) }; setCenter(next); }); map.on("error", () => setError("Map tiles could not be loaded. Check your network connection and try again.")); return () => { map.remove(); mapRef.current = null; }; }, []);
  useEffect(() => { if (!mapRef.current || center === INDIA_CENTER) return; const timer = window.setTimeout(() => void reverse(center.latitude, center.longitude), 700); return () => window.clearTimeout(timer); }, [center.latitude, center.longitude]);
  function moveTo(latitude: number, longitude: number, zoom = 15) { const next = { latitude: Number(latitude.toFixed(6)), longitude: Number(longitude.toFixed(6)) }; setCenter(next); mapRef.current?.flyTo({ center: [next.longitude, next.latitude], zoom }); }
  function applyAddress(value: Record<string, string> | undefined, display: string, normalized = false) { const a = value || {}; setAddress(normalized ? (display || [a.house_number, a.road].filter(Boolean).join(" ")) : display); setFields({ area: normalized ? (a.area_locality || "") : (a.suburb || a.neighbourhood || a.village || ""), city: normalized ? (a.city || "") : (a.city || a.town || a.municipality || ""), district: normalized ? (a.district || "") : (a.county || a.district || ""), state: normalized ? (a.state || "") : (a.state || ""), postal_code: normalized ? (a.postal_code || "") : (a.postcode || "") }); }
  async function search() { if (query.trim().length < 3) return; setSearching(true); setError(null); try { setResults(await mapService.search(`${query}, India`) as typeof results); } catch (err) { setError(err instanceof Error ? err.message : "Address search failed."); } finally { setSearching(false); } }
  async function selectResult(result: typeof results[number]) { const latitude = Number(result.lat), longitude = Number(result.lon); setResults([]); moveTo(latitude, longitude); await reverse(latitude, longitude); }
  async function reverse(latitude: number, longitude: number) { setReverseLoading(true); setError(null); try { const result = await mapService.reverseGeocode(latitude, longitude) as { display_name?: string; address?: Record<string, string>; area_locality?: string; city?: string; district?: string; state?: string; postal_code?: string }; applyAddress({ ...(result.address || {}), area_locality: result.area_locality || "", city: result.city || "", district: result.district || "", state: result.state || "", postal_code: result.postal_code || "" }, result.display_name || `${latitude}, ${longitude}`, true); } catch { setError("Address lookup failed. You can enter the address and PIN Code manually or retry."); } finally { setReverseLoading(false); } }
  function useCurrentLocation() { if (!navigator.geolocation) { setError("GPS is not available in this browser."); return; } navigator.geolocation.getCurrentPosition((position) => { const { latitude, longitude } = position.coords; moveTo(latitude, longitude); setError(null); void reverse(latitude, longitude); }, () => setError("GPS permission was denied. Search or pan the map to choose a location."), { enableHighAccuracy: true, timeout: 8000 }); }
  function updateField(name: keyof typeof fields, value: string) { setFields((current) => ({ ...current, [name]: name === "postal_code" ? value.replace(/\D/g, "").slice(0, 6) : value })); }
  const validPin = /^[1-9][0-9]{5}$/.test(fields.postal_code);
  function confirm() { if (!validPin) { setError("PIN Code must be exactly 6 digits and cannot start with 0."); return; } onConfirm({ ...center, address, ...fields }); }
  return <div className="rounded-lg border border-slab-border bg-white p-4 shadow-soft"><div className="mb-3 flex flex-col gap-2 sm:flex-row"><label className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-3 text-slab-muted" size={16} /><input className="min-h-11 w-full rounded-md border border-slab-border pl-10 pr-3 text-sm" placeholder="Search site address in India" value={query} onChange={(e) => setQuery(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") void search(); }} /></label><Button type="button" variant="secondary" onClick={() => void search()} disabled={searching}><Search size={16} /> {searching ? "Searching" : "Search"}</Button><Button type="button" variant="secondary" onClick={useCurrentLocation}><Navigation size={16} /> GPS</Button></div>{results.length ? <div className="mb-3 space-y-2 rounded-md border border-slab-border p-2">{results.map((result) => <button type="button" key={`${result.lat}-${result.lon}`} className="block w-full rounded p-2 text-left text-sm hover:bg-slate-50" onClick={() => void selectResult(result)}>{result.display_name}</button>)}</div> : null}<div className="relative h-[360px] overflow-hidden rounded-md border border-slab-border bg-slate-100"><div ref={mapNode} className="h-full w-full" />{!error ? <MapPin className="pointer-events-none absolute left-1/2 top-1/2 -ml-4 -mt-8 text-slab-primaryStrong drop-shadow" size={34} /> : <div className="absolute inset-0 flex items-center justify-center bg-white/85 p-6 text-center text-sm text-slab-error">{error}</div>}</div><div className="mt-3 grid gap-3 sm:grid-cols-2"><input className="min-h-11 w-full rounded-md border border-slab-border px-3 sm:col-span-2" value={address} onChange={(e) => setAddress(e.target.value)} placeholder="Address" /><input className="min-h-11 rounded-md border border-slab-border px-3" value={fields.area} onChange={(e) => updateField("area", e.target.value)} placeholder="Area / Locality" /><input className="min-h-11 rounded-md border border-slab-border px-3" value={fields.city} onChange={(e) => updateField("city", e.target.value)} placeholder="City" /><input className="min-h-11 rounded-md border border-slab-border px-3" value={fields.district} onChange={(e) => updateField("district", e.target.value)} placeholder="District" /><input className="min-h-11 rounded-md border border-slab-border px-3" value={fields.state} onChange={(e) => updateField("state", e.target.value)} placeholder="State" /><label className="text-sm font-semibold text-slab-ink sm:col-span-2">PIN Code<input className={`mt-1 min-h-11 w-full rounded-md border px-3 font-normal ${fields.postal_code && !validPin ? "border-red-400" : "border-slab-border"}`} inputMode="numeric" maxLength={6} value={fields.postal_code} onChange={(e) => updateField("postal_code", e.target.value)} placeholder="680001" required />{fields.postal_code && !validPin ? <span className="font-normal text-slab-error">Enter 6 digits; first digit cannot be 0.</span> : null}</label></div><div className="mt-3 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="text-sm text-slab-muted"><p className="font-semibold text-slab-ink">{reverseLoading ? "Finding address..." : address}</p><p>{center.latitude}, {center.longitude}</p>{error ? <p className="mt-1 text-slab-error">{error}</p> : null}</div><Button type="button" onClick={confirm} disabled={reverseLoading}>Confirm location</Button></div></div>;
}


